package main

// Version information
const (
	// Version is the current version of spannerclosecheck
	Version = "v0.1.0"

	// BuildDate is the date the binary was built (set via ldflags)
	BuildDate = "unknown"

	// GitCommit is the git commit hash (set via ldflags)
	GitCommit = "unknown"
)
